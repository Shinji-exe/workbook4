//1. Code that executes before the loop begins
// let count = 1;
let counter = 0
// while (count <= 100) {//2. A condition that must be true for the loop to be executing
//     console.log("hi " + count);
//     count = count+1 // or count++ //3. code that runs at the bottom of each iteration
// }

//1. Code that executes before the loop begins
//2. A condition that must be true for the loop to be executing
//3. code that runs at the bottom of each iteration

//1. Code that executes before the loop begins //2. A condition that must be true for the loop to be executing //3. code that runs at the bottom of each iteration
for(let i = 0; i <= 100; i++){
console.log("hi" + counter)
}